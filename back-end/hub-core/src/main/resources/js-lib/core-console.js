
function console_info(message) {
    //print(message);
}
function console_debug(message) {
    //print("{DEBUG}:: " + message);
}

var console = {
        info: console_info,
        debug: console_debug
};
